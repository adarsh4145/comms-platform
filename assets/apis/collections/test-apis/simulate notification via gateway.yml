info:
  name: simulate notification via gateway
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/upstreamsimulator/simulate/notification
  body:
    type: json
    data: '{ "recipient": "test@example.com", "message": "hello", "priority": "HIGH" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
