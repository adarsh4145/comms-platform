info:
  name: gateway-ingestionService call
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/upstreamsimulator/simulate/notification
  body:
    type: json
    data: |-
      {
        "recipient": "test@example.com",
        "message": "hello",
        "priority": "HIGH",
        "channel": "EMAIL",
        "from": "noreply@comms-platform.local"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
